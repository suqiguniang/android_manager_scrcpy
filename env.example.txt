# 环境变量配置示例
# 复制此文件为 .env 并根据实际情况修改

# 服务器配置
SERVER_HOST=0.0.0.0
SERVER_PORT=8080

# ADB 服务器配置
ADB_HOST=localhost
ADB_PORT=5037

# CORS 配置
CORS_ORIGIN=http://localhost:5173

# 安全配置（生产环境务必修改！）
COOKIE_SECRET=your-random-secret-here
SESSION_TOKEN=your-random-token-here

# 日志级别 (trace, debug, info, warn, error, fatal)
LOG_LEVEL=info

# 运行环境
NODE_ENV=development

